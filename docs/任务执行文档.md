# 就宠你 — 任务执行文档

> 最后更新：2026-06-09

---

## 一、已完成事务

### 核心基础设施
- [x] 微信云开发环境初始化（cloud1-9gkyi3gf77e7048e）
- [x] 项目基础架构搭建（app.js / app.json / app.wxss）
- [x] Vant Weapp 组件库集成（20 个全局组件）
- [x] 全局常量体系定义（角色、存储键、会话过期时间）
- [x] ESLint + Prettier 代码规范配置

### 认证系统
- [x] 微信一键登录（宠物主人）
- [x] 账号密码登录（三角色通用）
- [x] 宠物主人注册流程
- [x] 机构注册流程（含资质上传）
- [x] 默认管理员自动创建（admin / admin123）
- [x] 会话管理（本地缓存 + 云数据库校验，7 天过期）
- [x] 角色路由分发（switchTab / redirectTo）
- [x] 机构登录审核状态拦截
- [x] **修复退出登录自动回跳管理员 bug**（添加 `jcn_logged_out` 标记防止静默恢复）

### 宠物主人功能
- [x] 首页服务广场（推荐机构 + 推荐服务）
- [x] 浏览机构列表（搜索、筛选）
- [x] 浏览服务列表（分类筛选、价格区间筛选、搜索）
- [x] 机构详情（信息、服务列表）
- [x] 服务详情页（查看服务、选择宠物、笼位检查、下单、显示机构信息）
- [x] 宠物档案 CRUD（增删改查，子包 packagePet）
- [x] 25 种宠物预设头像选择
- [x] 宠物自定义头像上传（云存储）
- [x] 宠物展示设置（在档案页设为"展示宠物"，"我的"页面优先显示）
- [x] 用户订单中心（查看、筛选、确认完成、评价打分）
- [x] 订单详情（状态流转、图片预览）
- [x] 用户评价修改与删除（订单详情页 + 订单列表页）
- [x] 支付页（待支付倒计时、取消订单）
- [x] 个人资料编辑（头像、昵称、手机、邮箱、简介）
- [x] 底部导航栏三 Tab（首页 → 订单 → 我的）

### 健康管理模块（2026-06-09 完成）
- [x] **健康页面重构**（`pages/health/health.js`）
  - 自定义导航栏（固定定位，适配状态栏高度）
  - 宠物选择器（支持多宠物切换）
  - 健康状态卡（体重、疫苗、驱虫）
  - 体重趋势图（最近 7 次记录，柱状图）
  - 疫苗时间线（最近 5 条记录）
  - 驱虫时间线（最近 5 条记录）
  - 待办提醒（疫苗/驱虫到期提醒）
  - 快捷记录入口（体重、疫苗、驱虫、体检）
  - 最近记录列表（支持 6 种类型标签）
  - AI 健康建议（调用云函数分析）
- [x] **修复标题重复问题**：添加 `"navigationStyle": "custom"` 禁用微信默认导航栏
- [x] **修复宠物选择器不显示**：将显示条件从 `petList.length > 1` 改为 `petList.length > 0`
- [x] **修复宠物选择器被遮挡**：动态计算导航栏高度，设置正确的 padding-top

### 智能匹配模块（2026-06-09 完成）
- [x] **智能匹配页面**（`pages/smart-match/smart-match.js`）
  - 自定义导航栏
  - 可折叠筛选面板（更多筛选/收起筛选）
  - 多维度筛选条件：
    - 宠物类型（狗、猫、其他）
    - 宠物体型（小型、中型、大型）
    - 宠物年龄（幼年、成年、老年）
    - 服务类型（寄养、美容、医疗、上门、附加）
    - 价格范围（最低价、最高价）
    - 时间段（工作日、周末、节假日）
    - 位置距离（1km、3km、5km、10km）
    - 特殊需求（医疗护理、老年护理、幼宠照顾等）
    - 自定义需求（文本输入）
  - 智能匹配算法（多维度加权评分，总分 100 分）
  - 服务卡片列表展示
  - 筛选结果摘要

### 机构功能
- [x] 机构注册页面（完整表单 + 资质图片上传）
- [x] 机构首页数据看板（订单数、服务数统计）
- [x] 机构资料编辑（所有字段 + 笼位数量 + 环境图片）
- [x] 机构订单管理（四状态 Tab：待确认/进行中/待确认取回/已完成）
- [x] 订单状态流转（confirmed → in_progress → to_confirm → completed）
- [x] 笼位容量检查（接单前校验，防止超售）
- [x] 离开时间倒计时（60 秒刷新）
- [x] 宠物寄养状态同步
- [x] 服务管理（按类别分组：寄养/美容/医疗/上门/附加）
- [x] 添加/编辑服务（25 种模板、价格校验、图片上传）
- [x] 收入统计看板（总览、月度柱状图、状态饼图、分类收入、每日订单）
- [x] 评价管理（按服务/评分筛选、好评率、评分分布图、机构回复）
- [x] 机构宠物管理（寄养中的宠物列表）
- [x] 机构动态管理

### 管理员功能
- [x] 管理后台首页（角色守卫 + 导航）
- [x] 机构审核页面（待审核/已拒绝列表、查看详情、通过/拒绝）
- [x] 平台数据看板（Tab 切换：总览、审核状态、类型分布、收入统计）

### AI 功能
- [x] AI 健康分析云函数（调用 DeepSeek API，需配置 API_KEY）

### Bug 修复
- [x] **机构详情服务不显示**：`resolveTempUrls` 未导入导致 ReferenceError → 已修复
- [x] **订单图片不显示**：存储过期临时 URL → 改为存储 cloud:// ID，显示时解析
- [x] **订单图片 403 错误**：添加图片加载失败兜底处理
- [x] **退出登录自动回跳管理员**：`checkLogin()` 静默云查询 → 添加 `jcn_logged_out` 标记
- [x] **健康页面标题重复**：添加 `"navigationStyle": "custom"` 配置
- [x] **健康页面宠物选择器不显示**：修改显示条件为 `petList.length > 0`
- [x] **健康页面宠物选择器被遮挡**：动态计算导航栏高度

### 代码优化
- [x] **统一 nav-bar 标题样式**：在 `app.wxss` 添加全局 `.nav-bar__title`，删除重复定义
- [x] **统一自定义导航栏样式**：登录页、浏览机构页、浏览服务页等统一橙色渐变导航栏
- [x] **提取重复工具函数**：创建 `utils/helpers.js`，提取 `formatDate`、`buildPetInfoText`、`buildLeaveRemainText`、`isLeaveExpired`、`CAT_TITLE_MAP`
- [x] **清理死代码**：删除 `app.js` 中未使用的 `callService` 方法
- [x] **清理调试日志**：删除 `authService.js` 中的 `console.log` 调试语句
- [x] **删除冗余文件**：
  - 5 个空壳页面（agency/order_list、ai/creation、health/health_stats、match/match_list、profile/pet_archive）
  - 10 个未注册 TDesign 模板页面（home、dataCenter、loginCode、release、search、setting、chat、message、my/index、my/info-edit）
  - 未使用工具文件（utils/util.js、utils/eventBus.js、config/index.js、api/request.js、behaviors/）
  - ~120 个未使用静态 PNG 图标（保留实际使用的 18 个文件）
  - `custom-tab-bar/` 目录（已被原生 TabBar 替代）
  - `mock/` 目录全部文件
- [x] **首页显示机构名称**：在服务卡片中显示所属机构名称
- [x] **服务详情显示机构信息**：在服务详情页添加机构信息卡片

### 文档更新（2026-06-09）
- [x] 创建 `agents/` 文件夹和 README
- [x] 创建 `docs/设计规范.md`（UI/UX 设计规范）
- [x] 创建 `docs/开发规范.md`（代码编写规范）
- [x] 创建 `docs/页面功能说明.md`（所有页面功能说明）
- [x] 更新 `docs/项目结构文档.md`（添加新页面和技术债务）
- [x] 更新 `docs/任务执行文档.md`（本文档）

---

## 二、进行中 / 部分完成事务

### 智能匹配算法
- [ ] **匹配算法优化**：当前算法已实现但需要根据实际数据调优权重

### 宠物档案已知 Bug
- [ ] **数据隔离问题**：`loadPetList()` 未按用户过滤，所有用户可见所有宠物数据

---

## 三、待办事务

### 高优先级 — 功能完善

- [x] **修复宠物档案数据隔离**：`packagePet/pages/pet/pet.js` 的 `loadPetList()` 已添加 `ownerId` 过滤条件
- [x] **修复云函数依赖**：`cloudfunctions/ai_handler/package.json` 已添加 `axios` 依赖

### 中优先级 — 功能增强

- [ ] **AI 创作页面**：当前为空壳，需实现实际的 AI 创作功能
- [ ] **机构曝光/推广功能**（agency 首页有入口，显示"开发中"）
- [ ] **动态计算机构评分**：当前 `agency.score` 为静态字段，应根据用户评价动态计算
- [ ] **健康数据导出**：支持导出宠物健康报告

### 低优先级 — 遗留代码清理

- [x] **清理 TDesign 模板遗留组件**：
  - `components/card/`、`components/nav/` 已删除
- [x] **清理未使用的静态图标**：
  - 已删除 `pet/contact.png`、`tab-ask-orange.png`、`tab-cart-orange.png`、`tab-pet-green.png`
  - `static/tab-ask-orange.png`、`static/tab-cart-orange.png`、`static/tab-pet-green.png`、`static/tab-pet-orange.png`（不在当前 tabBar 配置中）
- [ ] **评估 `tdesign-miniprogram` 残留引用**：如无页面使用，可从组件 JSON 中移除

### 远期规划

- [ ] 实时视频看宠
- [ ] 智能喂食提醒
- [ ] 宠物社交功能
- [ ] 机构排行榜

---

## 四、技术债务清单

| 问题 | 位置 | 严重程度 | 说明 |
|------|------|---------|------|
| 宠物档案无用户过滤 | `packagePet/pages/pet/pet.js` → `loadPetList()` | 高 | 数据泄露风险，所有用户可见全部宠物 |
| 云函数缺少 axios 依赖 | `cloudfunctions/ai_handler/package.json` | 中 | `require('axios')` 会失败，需声明依赖或手动安装 |
| TDesign 组件残留引用 | `components/card/`、`components/nav/` | 低 | 引用 tdesign-miniprogram 但未安装、未使用 |
| 未使用的静态图标 | `static/tab-ask-*`、`static/tab-cart-*`、`static/tab-pet-*` | 低 | 增加包体积，建议删除 |

---

## 五、最近变更记录

### 2026-06-09
- 完成健康管理页面重构（体重趋势图、疫苗/驱虫时间线、待办提醒）
- 修复健康页面标题重复问题
- 修复健康页面宠物选择器不显示问题
- 创建智能匹配页面（多维度筛选、智能匹配算法）
- 首页和服务详情页显示机构信息
- 创建项目文档体系（设计规范、开发规范、页面功能说明）
- 创建 agents 文件夹和 AI 助手配置
- 修复宠物档案数据隔离问题（添加 ownerId 过滤）
- 修复云函数缺少 axios 依赖
- 删除 TDesign 残留组件（components/card/、components/nav/）
- 删除未使用的静态资源（contact.png、tab-ask-*、tab-cart-*、tab-pet-green）
- 修复 wx.getSystemInfoSync 弃用警告（迁移到 getStatusBarHeight 工具函数）

### 2026-06-03
- 修复退出登录自动回跳管理员 bug
- 统一自定义导航栏样式
- 清理死代码和冗余文件
- 完成机构评价管理功能

### 2026-05-26
- 首页 UI 重设计（温暖治愈风）
- 完成宠物档案 CRUD 功能
- 完成订单中心功能
