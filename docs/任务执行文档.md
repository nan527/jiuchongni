# 就宠你 — 任务执行文档

> 最后更新：2026-05-26

---

## 一、已完成事务

### 核心基础设施
- [x] 微信云开发环境初始化（cloud1-9gkyi3gf77e7048e）
- [x] 项目基础架构搭建（app.js / app.json / app.wxss）
- [x] Vant Weapp 组件库集成（20 个全局组件）
- [x] 全局常量体系定义（角色、存储键、会话过期时间）
- [x] ESLint + Prettier 代码规范配置

### 认证系统
- [x] 微信一键登录（宠物主人）
- [x] 账号密码登录（三角色通用）
- [x] 宠物主人注册流程
- [x] 机构注册流程（含资质上传）
- [x] 默认管理员自动创建（admin / admin123）
- [x] 会话管理（本地缓存 + 云数据库校验，7 天过期）
- [x] 角色路由分发（switchTab / redirectTo）
- [x] 机构登录审核状态拦截

### 宠物主人功能
- [x] 首页服务广场（加载寄养/领养/机构服务数据）
- [x] 宠物档案 CRUD（增删改查）
- [x] 25 种宠物预设头像选择
- [x] 宠物自定义头像上传（云存储）
- [x] 发布寄养需求（选宠物、填表单、传图片）
- [x] 发布领养信息（选宠物、填表单、传图片）
- [x] 寄养/领养详情页（查看、编辑、关闭、删除）
- [x] 寄养/领养申请流程（创建 foster_applications + user_orders）
- [x] 服务详情页（查看服务、选择宠物、笼位检查、下单）
- [x] 用户订单中心（查看、筛选、确认完成、评价打分）
- [x] 寄养中心（我的发布、我参与的、机构订单 三 Tab）
- [x] 个人资料编辑（头像、昵称、手机、邮箱、简介）
- [x] 社区论坛（浏览帖子、全部/我的 Tab、点赞）
- [x] 发帖功能（标题、内容、图片、话题标签）
- [x] 帖子详情 + 评论功能

### 机构功能
- [x] 机构注册页面（完整表单 + 资质图片上传）
- [x] 机构首页数据看板（订单数、服务数统计）
- [x] 机构资料编辑（所有字段 + 笼位数量 + 环境图片）
- [x] 机构订单管理（四状态 Tab：待确认/进行中/待确认取回/已完成）
- [x] 订单状态流转（confirmed → in_progress → to_confirm → completed）
- [x] 笼位容量检查（接单前校验，防止超售）
- [x] 离开时间倒计时（60 秒刷新）
- [x] 宠物寄养状态同步
- [x] 服务管理（按类别分组：寄养/美容/医疗/上门/附加）
- [x] 添加/编辑服务（25 种模板、价格校验、图片上传）
- [x] 收入统计看板（总览、月度柱状图、状态饼图、分类收入、每日订单）
- [x] 评价管理（按服务/评分筛选、好评率、评分分布图）

### 管理员功能
- [x] 管理后台首页（角色守卫 + 导航）
- [x] 机构审核页面（待审核/已拒绝列表、查看详情、通过/拒绝）

### AI 功能
- [x] AI 健康分析云函数（调用 DeepSeek API，需配置 API_KEY）

---

## 二、进行中 / 部分完成事务

### 健康管理模块
- [ ] **健康页面**（`pages/health/health.js`）
  - 已完成：加载宠物信息、获取健康历史、保存健康数据、调用 AI 分析
  - 未完成：依赖 `app.globalData.pet.currentPet` 但该值从未被设置，导致页面无法正常工作
  - 未完成：健康统计子页面（`health_stats.js`）为空壳

### 智能匹配模块
- [ ] **匹配页面**（`pages/match/match.js`）
  - 已完成：加载宠物数据、加载机构数据、定义匹配函数（性格匹配、品种匹配、权重计算）
  - 未完成：`calculateMatch()` 硬编码 `match_score: 95`，未使用已定义的匹配算法
  - 未完成：引用 `app.globalData.pet` 但该对象不存在
  - 未完成：匹配结果子页面（`match_list.js`）为空壳

### 宠物档案已知 Bug
- [ ] **数据隔离问题**：`loadPetList()` 未按用户过滤，所有用户可见所有宠物数据

---

## 三、待办事务

### 高优先级 — 功能完善

- [ ] **修复宠物档案数据隔离**：`pages/pet/pet.js` 的 `loadPetList()` 需添加 `_openid` 过滤条件
- [ ] **修复健康页面入口**：确保 `globalData.pet.currentPet` 在导航到健康页前被正确设置
- [ ] **修复匹配算法**：`pages/match/match.js` 的 `calculateMatch()` 应调用已定义的 `characterMatch`、`speciesMatch`、`scoreWeight` 函数
- [ ] **创建 `services/userService.js`**：README 和 AGENTS.md 中提及但从未创建

### 中优先级 — TDesign 遗留页面清理或重写

- [ ] **AI 创作页面**（`pages/ai/`）：当前为空壳，需实现实际的 AI 创作功能
- [ ] **聊天功能**（`pages/chat/`）：当前使用 Mock WebSocket，需接入真实即时通讯方案
- [ ] **消息列表**（`pages/message/`）：当前使用 Mock 数据，需接入真实消息系统
- [ ] **数据中心**（`pages/dataCenter/`）：当前使用 Mock 接口，需实现真实数据统计
- [ ] **搜索功能**（`pages/search/`）：当前使用 Mock 接口，需实现真实搜索
- [ ] **设置页面**（`pages/setting/`）：当前为静态菜单，需实现实际设置功能
- [ ] **短信验证码登录**（`pages/loginCode/`）：当前使用 Mock 接口
- [ ] **个人信息编辑**（`pages/my/info-edit/`）：当前使用 Mock 接口，可考虑移除（已有 profile 页面）

### 低优先级 — 遗留代码清理

- [ ] **清理 TDesign 模板遗留文件**：
  - `components/card/`、`components/nav/`（未使用的组件）
  - `custom-tab-bar/`（已被原生 TabBar 替代）
  - `mock/` 目录全部文件
  - `api/request.js`（业务页未使用）
  - `behaviors/useToast.js`（仅遗留页面使用）
  - `config.js`、`config/index.js`（遗留配置）
  - `static/Avatar/` 下的遗留用户头像
  - `static/user-bg.png`、`vip-card.png`、`top.png`
- [ ] **清理 TDesign 模板遗留页面**：
  - `pages/home/`（已被 `pages/index` 替代）
  - `pages/release/`（空壳）
  - `pages/loginCode/`（未接入）
  - `pages/setting/`（无功能）
  - `pages/dataCenter/`（Mock）
  - `pages/search/`（Mock）
  - `pages/chat/`（Mock）
  - `pages/message/`（Mock）
  - `pages/my/info-edit/`（Mock）
- [ ] **评估 `tdesign-miniprogram` 依赖**：如无页面使用，可从 `package.json` 移除

### 远期规划（README 中提及）

- [ ] AI 宠物健康分析（待接入 — 云函数已就绪，需配置 API Key）
- [ ] 实时视频看宠
- [ ] 智能喂食提醒
- [ ] 机构曝光/推广功能（agency 首页有入口，显示"开发中"）
- [ ] 机构宠物管理功能（agency 首页有入口，显示"开发中"）

---

## 四、技术债务清单

| 问题 | 位置 | 严重程度 | 说明 |
|------|------|---------|------|
| 宠物档案无用户过滤 | `pages/pet/pet.js` → `loadPetList()` | 高 | 数据泄露风险，所有用户可见全部宠物 |
| globalData.pet 未初始化 | `app.js` → `globalData` | 高 | 健康页和匹配页依赖此对象 |
| 匹配算法未生效 | `pages/match/match.js` → `calculateMatch()` | 中 | 硬编码分数，已有算法未调用 |
| userService.js 缺失 | `services/` | 中 | 文档提及但未创建 |
| TDesign 模板遗留代码 | 多处 | 低 | 增加维护成本，建议统一清理 |
| 双版本个人中心 | `pages/my/my.js` + `pages/my/index.js` | 低 | 存在两套实现，需确认哪个是主版本 |
