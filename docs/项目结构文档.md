# 就宠你 — 项目结构文档

## 一、项目概览

| 项目 | 说明 |
|------|------|
| 项目名称 | 就宠你（JCN）— 宠物智能服务平台 |
| 项目类型 | 微信小程序 |
| 前端框架 | 微信小程序原生（WXML / WXSS / JS） |
| UI 组件库 | Vant Weapp ^1.11.7 |
| 样式方案 | WXSS + LESS + CSS 变量 |
| 后端服务 | 微信云开发（云函数 + 云数据库） |
| 存储 | 微信云存储 |
| 认证方式 | 微信登录 + 账号密码双模式 |
| 品牌色 | #FF9800（暖橙色） |

---

## 二、三大角色体系

### 1. 宠物主人（pet_owner）

**定位：** 智能寄养、健康管理、AI 创作

**入口页面：** `/pages/index/index`（Tab 页面）

| 功能模块 | 页面路径 | 说明 |
|---------|---------|------|
| 首页服务广场 | `pages/index` | 展示推荐机构、推荐服务 |
| 浏览机构 | `pages/browse-agencies` | 机构列表、搜索筛选 |
| 机构详情 | `pages/agency-detail` | 机构信息、服务列表 |
| 服务详情 | `pages/service-detail` | 查看服务、选择宠物、笼位检查、下单 |
| 宠物档案 | `packagePet/pages/pet` | 宠物 CRUD、25 种预设头像、自定义上传 |
| 宠物详情 | `pages/pet-detail` | 宠物详情、健康记录 |
| 订单中心 | `pages/orders` | 查看订单、筛选、确认完成、评价打分 |
| 订单详情 | `pages/order-detail` | 订单详情、状态流转 |
| 支付页 | `pages/payment` | 待支付倒计时、取消订单 |
| 个人中心 | `pages/my` | 个人信息、导航入口 |
| 个人资料 | `pages/profile` | 编辑头像、昵称、手机、邮箱、简介 |
| 健康管理 | `pages/health` | 健康记录、AI 分析（部分实现） |

### 2. 寄养机构（agency）

**定位：** 订单处理、环境打卡、客户管理

**入口页面：** `/pages/agency/agency`（非 Tab）

| 功能模块 | 页面路径 | 说明 |
|---------|---------|------|
| 机构首页 | `pages/agency` | 数据看板（订单数、服务数），导航入口 |
| 机构注册 | `pages/agency-register` | 完整注册表单：机构信息、资质上传、账号密码 |
| 机构编辑 | `pages/agency-edit` | 编辑机构资料、笼位数量、环境图片 |
| 浏览机构 | `pages/browse-agencies` | 机构列表（共用） |
| 机构详情 | `pages/agency-detail` | 机构信息（共用） |
| 订单管理 | `pages/agency-orders` | 四 Tab：待确认/进行中/待确认取回/已完成，笼位容量检查 |
| 服务管理 | `pages/agency-services` | 服务列表、按类别分组 |
| 添加服务 | `pages/agency-services-add` | 25 种服务模板、价格校验、图片上传 |
| 收入统计 | `pages/agency-revenue` | 总览统计、月度柱状图、订单状态饼图、分类收入、每日订单 |
| 评价管理 | `pages/agency-reviews` | 评价列表、按服务/评分筛选、好评率、评分分布 |

### 3. 平台管理员（admin）

**定位：** 资质审核、系统监控、全局看板

**入口页面：** `/pages/admin/admin`（非 Tab）

| 功能模块 | 页面路径 | 说明 |
|---------|---------|------|
| 管理后台 | `pages/admin` | 管理员首页，角色守卫，导航入口 |
| 机构审核 | `pages/admin/audit` | 待审核/已拒绝列表，查看详情，通过/拒绝操作 |

> 默认管理员账号：`admin` / `admin123`（首次启动自动创建）

---

## 三、角色路由与认证机制

```
启动 → app.js silentLogin()
         ↓
    authService.checkLogin()
         ↓
    ┌─ 已主动登出（jcn_logged_out 标记）→ 返回 null
    ├─ 本地缓存有效 → 直接跳转对应角色首页
    └─ 缓存过期/不存在 → 查询云数据库 users 集合
                            ↓
                    ┌─ 找到 → 刷新缓存，跳转
                    └─ 未找到 → 停在登录页
```

| 角色 | 登录方式 | 路由方式 | 特殊要求 |
|------|---------|---------|---------|
| 宠物主人 | 微信一键登录 / 账号密码 | `wx.switchTab` | 无 |
| 机构 | 账号密码 | `wx.redirectTo` | 必须 `auditStatus === 'approved'` |
| 管理员 | 账号密码 | `wx.redirectTo` | 无 |

---

## 四、项目文件结构

```
jcn/
├── app.js                          # 应用入口，初始化云环境，静默登录
├── app.json                        # 全局配置：页面路由，Tab 底栏，Vant 组件
├── app.wxss                        # 全局样式（CSS 变量、nav-bar__title 等）
├── app.less                        # LESS 变量（品牌色定义）
├── project.config.json             # 微信开发者工具配置
├── package.json                    # 依赖：@vant/weapp
├── .eslintrc.js                    # ESLint 配置
├── sitemap.json                    # 微信站点地图
│
├── constants/                      # 常量定义
│   └── index.js                    #   角色常量、存储键名、会话过期时间
│
├── services/                       # 业务服务层
│   └── authService.js              #   认证服务（登录、注册、会话管理、角色路由）
│
├── utils/                          # 工具函数
│   ├── fileHelper.js               #   云文件临时 URL 解析
│   └── helpers.js                  #   共享工具函数（formatDate、buildPetInfoText 等）
│
├── cloudfunctions/                 # 云函数
│   └── ai_handler/                 #   AI 健康分析 + 文件 URL 解析
│       └── index.js
│
├── pages/                          # ==================== 页面目录 ====================
│   │
│   │  ── 宠物主人页面 ──
│   ├── index/                      # 首页服务广场
│   ├── browse-agencies/            # 浏览机构列表
│   ├── agency-detail/              # 机构详情 + 服务列表
│   ├── service-detail/             # 服务详情 + 下单
│   ├── orders/                     # 用户订单中心
│   ├── order-detail/               # 订单详情
│   ├── payment/                    # 支付页（待支付倒计时）
│   ├── pet-detail/                 # 宠物详情 + 健康记录
│   ├── health/                     # 健康管理（部分实现）
│   ├── profile/                    # 个人资料编辑
│   ├── my/                         # 个人中心
│   │
│   │  ── 宠物档案子包 ──
│   ├── packagePet/pages/pet/       # 宠物档案管理（CRUD）
│   │
│   │  ── 寄养机构页面 ──
│   ├── agency/                     # 机构首页（数据看板）
│   ├── agency-register/            # 机构注册
│   ├── agency-edit/                # 机构资料编辑
│   ├── agency-orders/              # 机构订单管理
│   ├── agency-services/            # 机构服务列表
│   ├── agency-services-add/        # 添加/编辑服务
│   ├── agency-revenue/             # 收入统计
│   ├── agency-reviews/             # 评价管理
│   │
│   │  ── 管理员页面 ──
│   ├── admin/                      # 管理后台 + audit 审核页面
│   │
│   │  ── 登录页面 ──
│   ├── login/                      # 登录页（三角色、双模式）
│   │
│   │  ── 部分实现的页面 ──
│   ├── match/                      # 智能匹配（匹配算法未生效）
│   │
├── mock/                           # Mock 数据层（TDesign 模板遗留）
│
└── static/                         # 静态资源
    ├── pet/                        #   logo、联系图标、空状态图
    │   ├── logo.png
    │   ├── contact.png
    │   └── mescroll-empty.png
    ├── Avatar/                     #   用户头像（5 张）
    ├── tab-home.png                #   TabBar 图标（首页）
    ├── tab-home-orange.png         #   TabBar 图标（首页选中）
    ├── tab-my.png                  #   TabBar 图标（我的）
    ├── tab-my-orange.png           #   TabBar 图标（我的选中）
    ├── tab-pet-green.png           #   功能图标
    ├── tab-pet-orange.png          #   功能图标
    ├── tab-ask-orange.png          #   功能图标
    ├── tab-cart-orange.png         #   功能图标
    ├── missing-face.png            #   默认占位头像
    └── user-bg.png                 #   机构页背景
```

---

## 五、云数据库集合

| 集合名称 | 使用者 | 用途 |
|---------|--------|------|
| `users` | authService, profile, admin/audit | 用户账号（所有角色） |
| `agency_profiles` | authService, agency-edit, agency-orders, agency-detail, service-detail | 机构商业档案 |
| `pets` | pet, service-detail, health, match | 宠物档案 |
| `user_orders` | orders, agency-orders, agency-revenue, agency-reviews, service-detail | 全部订单 |
| `agency_services` | index, agency-services, agency-services-add, agency-detail, service-detail | 机构服务列表 |
| `health_records` | health | 宠物健康数据 |
| `posts` | forum, post, post-detail | 社区论坛帖子 |
| `comments` | post-detail | 帖子评论 |

---

## 六、全局注册的 Vant Weapp 组件

`van-button`、`van-cell`、`van-cell-group`、`van-field`、`van-icon`、`van-image`、`van-loading`、`van-tabbar`、`van-tabbar-item`、`van-uploader`、`van-search`、`van-grid`、`van-grid-item`、`van-card`、`van-tag`、`van-radio`、`van-radio-group`、`van-checkbox`、`van-checkbox-group`、`van-divider`

各页面可在自身 `.json` 中局部注册额外的 Vant 组件。

---

## 七、共享工具函数（utils/helpers.js）

| 函数 | 说明 | 使用页面 |
|------|------|---------|
| `CAT_TITLE_MAP` | 服务分类标题映射 | index, agency-detail, service-detail |
| `formatDate(t)` | 格式化日期为 YYYY-MM-DD | orders, agency-orders, service-detail |
| `formatDateTime(t)` | 格式化日期时间为 YYYY-MM-DD HH:mm | order-detail |
| `buildPetInfoText(petInfo)` | 构建宠物信息文本（品种 · 年龄） | orders, order-detail, payment |
| `buildLeaveRemainText(leaveTimeMs)` | 构建离开剩余时间文本 | orders |
| `isLeaveExpired(leaveTimeMs)` | 判断是否已超过离开时间 | orders |
